    10
  /    \
 6      14
/ \    /  \
4   8  12  16

// ex.
// node.val === 10
// node.left === { val: 6, left, right }
// node.right === { val: 14, left, right }

const isValid = (node: { left, right }) => {
  
  left.val > node.val return false;
  node = left

  right.val < node.val return false;
  node = right
};
